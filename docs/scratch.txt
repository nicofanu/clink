- Need to find out how to parse arguments.
    Module choices are:
    * argparse
    * getopts
    * optparse
  *Decided to go with argparse.

- Basic parameter scheme
    -a, --add
    -l, --list
    -d, --del
    --find
    -t, --title
    -l, --link
    -c, --tag
